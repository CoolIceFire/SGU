#include <cstdio>
#include <cstring>

int main()
{
	int n, i;

	while (~scanf("%d", &n))
	{
		if (n <= 8) printf("0\n");
		else
		{
			if (n == 9) printf("8\n");
			else
			{
				printf("72");
				for (i = 10; i < n; i++) printf("0");
				printf("\n");
			}
		}
	}

	return 0;
}
